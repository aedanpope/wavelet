{
  "id": "worksheet-2",
  "title": "Worksheet 2: Storing Your Numbers",
  "description": "Learn to use variables to store and manipulate data in Python.",
  "problems": [
    {
      "title": "Your first variable",
      "content": "Variables are like boxes that store values. This code stores the number 7 in a variable and prints it.",
      "task": "Run the code to see the value stored in the variable.",
      "starterCode": "age = 7\nprint(age)",
      "expectedOutput": "7",
      "hint": "The variable 'age' holds the number 7.",
      "validation": {
        "type": "exact_match",
        "rules": [
          {
            "type": "code_contains",
            "pattern": "age = 7"
          },
          {
            "type": "code_contains",
            "pattern": "print(age)"
          },
          {
            "type": "output_contains",
            "pattern": "7"
          }
        ]
      }
    },
    {
      "title": "Changing a variable",
      "content": "You can change what a variable stores by giving it a new value.",
      "task": "Run the code to see the updated value.",
      "starterCode": "score = 10\nscore = 15\nprint(score)",
      "expectedOutput": "15",
      "hint": "The second assignment replaces the first value.",
      "validation": {
        "type": "exact_match",
        "rules": [
          {
            "type": "output_contains",
            "pattern": "15"
          }
        ]
      }
    },
    {
      "title": "Two variables",
      "content": "Variables can store different values and be used together.",
      "task": "Run the code to see both values printed.",
      "starterCode": "apples = 5\nbananas = 3\nprint(apples)\nprint(bananas)",
      "expectedOutput": "5\n3",
      "hint": "Each print shows the value of one variable.",
      "validation": {
        "type": "exact_match",
        "rules": [
          {
            "type": "output_contains",
            "pattern": "5"
          },
          {
            "type": "output_contains",
            "pattern": "3"
          }
        ]
      }
    },
    {
      "title": "Variables in calculations",
      "content": "You can use variables in calculations.",
      "task": "Run the code to see the sum of apples and bananas.",
      "starterCode": "apples = 5\nbananas = 3\ntotal = apples + bananas\nprint(total)",
      "expectedOutput": "8",
      "hint": "Variables work like numbers in math.",
      "validation": {
        "type": "exact_match",
        "rules": [
          {
            "type": "output_contains",
            "pattern": "8"
          }
        ]
      }
    },
    {
      "title": "Edit the numbers",
      "content": "Change the numbers in the variables so the total is 20.",
      "task": "Edit the code to make total = 20.",
      "starterCode": "x = 5\ny = 7\ntotal = x + y\nprint(total)",
      "expectedOutput": "20",
      "hint": "Adjust x and y to add up to 20.",
      "validation": {
        "type": "pattern_match",
        "rules": [
          {
            "type": "output_contains",
            "pattern": "20"
          }
        ]
      }
    },
    {
      "title": "From input to variable",
      "content": "You can store what the user enters into a variable.",
      "task": "Enter a number and run the code.",
      "inputs": [
        {
          "name": "first_number",
          "label": "Enter a number:",
          "type": "number"
        }
      ],
      "starterCode": "num = first_number\nprint(num)",
      "expectedOutput": "dynamic",
      "hint": "Whatever you type will be printed.",
      "validation": {
        "type": "pattern_match",
        "rules": [
          {
            "type": "code_contains",
            "pattern": "num = first_number"
          }
        ]
      }
    },
    {
      "title": "Adding two inputs",
      "content": "Enter two numbers and the program will add them.",
      "task": "Fill in the inputs and run.",
      "inputs": [
        {
          "name": "a",
          "label": "First number:",
          "type": "number"
        },
        {
          "name": "b",
          "label": "Second number:",
          "type": "number"
        }
      ],
      "starterCode": "x = a\ny = b\nresult = x + y\nprint(result)",
      "expectedOutput": "dynamic",
      "hint": "The sum will be printed.",
      "validation": {
        "type": "pattern_match",
        "rules": [
          {
            "type": "code_contains",
            "pattern": "result = x + y"
          }
        ]
      }
    },
    {
      "title": "Multiplying two inputs",
      "content": "Enter two numbers and modify the code so it multiplies the variables instead of adding them.",
      "task": "Edit the code to multiply the variables.",
      "inputs": [
        {
          "name": "a",
          "label": "First number:",
          "type": "number"
        },
        {
          "name": "b",
          "label": "Second number:",
          "type": "number"
        }
      ],
      "starterCode": "x = a\ny = b\nresult = x + y\nprint(result)",
      "expectedOutput": "dynamic",
      "hint": "Change the + to * to multiply instead of add.",
      "validation": {
        "type": "pattern_match",
        "rules": [
          {
            "type": "code_contains",
            "pattern": "result = x * y"
          }
        ]
      }
    },
    {
      "title": "Double from input",
      "content": "Write a program that asks for a number and prints double its value.",
      "task": "Enter a number and write the program from scratch.",
      "inputs": [
        {
          "name": "n",
          "label": "Number:",
          "type": "number"
        }
      ],
      "starterCode": "",
      "expectedOutput": "dynamic",
      "hint": "Assign the input to a variable, multiply by 2, print.",
      "validation": {
        "type": "pattern_match",
        "rules": [
          {
            "type": "code_contains",
            "pattern": "* 2"
          }
        ]
      }
    },
    {
      "title": "Three times the fun",
      "content": "Enter a number and the program will print triple its value.",
      "task": "Enter a number.",
      "inputs": [
        {
          "name": "n",
          "label": "Number:",
          "type": "number"
        }
      ],
      "starterCode": "num = n\ntriple = num * 3\nprint(triple)",
      "expectedOutput": "dynamic",
      "hint": "Multiply the input by 3.",
      "validation": {
        "type": "pattern_match",
        "rules": [
          {
            "type": "code_contains",
            "pattern": "* 3"
          }
        ]
      }
    }
  ],
  "metadata": {
    "created": "2025-08-14"
  }
}