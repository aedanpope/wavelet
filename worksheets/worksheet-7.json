{
  "id": "worksheet-7",
  "title": "Worksheet 7: Functions",
  "description": "Make your own Python tools using def, parameters, and return. Then put them together.",
  "problems": [
    {
      "type": "concept",
      "icon": "🔧",
      "title": "Python comes with <em>functions</em>. Today you make your own",
      "content": "<p>You've already used Python's built-in <strong>functions</strong>, tools like <code>print()</code>, <code>len()</code>, and <code>range()</code> that come with Python.<br>The keyword <b>def</b> (short for <em>define</em>) lets you build your own:</p><pre style='background:#272822;color:#f8f8f2;padding:10px 14px;border-radius:6px;margin:8px 0;'><span style='color:#f92672'>def</span> <span style='color:#fd971f'>say_hi</span>():           <span style='color:#cccccc'>&larr; define</span>\n  <span style='color:#66d9ef'>print</span>(<span style='color:#e6db74'>\"Hi there!\"</span>)    <span style='color:#cccccc'>&larr; specify</span>\n\nsay_hi()                <span style='color:#cccccc'>&larr; call, now \"Hi there!\" gets printed</span></pre><p>The function specification must be <strong>indented</strong> &mdash; that's how Python knows which lines belong to the function. Nothing happens until you <em>call</em> it.</p>",
      "footer": "A function is a named piece of code you can call whenever you want."
    },
    {
      "title": "Your first function",
      "content": "<p>This program <strong>defines</strong> a function called <code>say_hi</code> and then <strong>calls</strong> it. Look at the two parts:</p><ul><li><code>def say_hi():</code> creates the function</li><li><code>say_hi()</code> calls the function</li></ul><p>The code under <code>def</code> only runs when you <em>call</em> the function with <code>say_hi()</code>.</p>",
      "task": "Run the code and watch <code>say_hi()</code> get called.",
      "starterCode": "def say_hi():\n  print(\"Hi there!\")\n\nsay_hi()",
      "hint": "Just press 'Run Code'. The function prints once because it's called once.",
      "codeHeight": 5,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def say_hi", "description": "Keep the def say_hi() line"},
          {"type": "code_contains", "pattern": "say_hi()", "description": "Call the function with say_hi()"},
          {"type": "output_not_empty"}
        ]
      }
    },
    {
      "title": "Call it more than once",
      "content": "<p>Once a function is defined, you can call it as many times as you like. Each call runs the body again.</p>",
      "task": "Add two more <code>say_hi()</code> calls so the message prints three times in total.",
      "starterCode": "def say_hi():\n  print(\"Hi there!\")\n\nsay_hi()\n",
      "hint": "Just write <code>say_hi()</code> on two new lines below.",
      "codeHeight": 6,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {
            "type": "solution_code",
            "solutionCode": "def say_hi():\n  print(\"Hi there!\")\n\nsay_hi()\nsay_hi()\nsay_hi()"
          }
        ]
      }
    },
    {
      "title": "Write your own function",
      "content": "<p>Your turn. Make a function called <code>cheer</code> that prints <code>\"Hooray!\"</code>, then call it.</p>",
      "task": "Define <code>cheer</code> and call it once.",
      "starterCode": "# Define cheer here\n\n\n# Call it here\n",
      "hint": "Start with a <code>def</code> line for <code>cheer</code>. Indent a <code>print</code> of <code>\"Hooray!\"</code> on the next line. After your function, add a line that calls it.",
      "codeHeight": 6,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def cheer", "description": "Define a function called cheer"},
          {"type": "ast_has_function_call", "function": "cheer", "description": "Call your function by writing cheer() on its own line"},
          {"type": "output_contains", "pattern": "Hooray", "description": "The function should print Hooray"}
        ]
      }
    },
    {
      "type": "concept",
      "icon": "📥",
      "title": "Parameters: your tool takes an input",
      "content": "<p>Your functions can take inputs too, the input goes in the brackets after the function name:</p><pre style='background:#272822;color:#f8f8f2;padding:10px 14px;border-radius:6px;margin:8px 0;'><span style='color:#f92672'>def</span> <span style='color:#fd971f'>greet</span>(name):    <span style='color:#cccccc'>&larr; add a parameter called 'name' to the function definition</span>\n  <span style='color:#66d9ef'>print</span>(<span style='color:#e6db74'>'Hi,'</span>, name)\n\ngreet(<span style='color:#e6db74'>'Ali'</span>)    <span style='color:#cccccc'># Hi, Ali</span>\ngreet(<span style='color:#e6db74'>'Sam'</span>)    <span style='color:#cccccc'># Hi, Sam</span></pre><p>When you call the function, the value you pass becomes that parameter inside the function.</p>",
      "footer": "Same function. Different inputs. Different output."
    },
    {
      "title": "Greet two friends",
      "content": "<p>Look at how <code>greet</code> uses the parameter <code>name</code>. Each call sends a different value in.</p>",
      "task": "Run the code and watch how the same function produces two different greetings.",
      "starterCode": "def greet(name):\n  print('Hi,', name)\n\ngreet('Ali')\ngreet('Sam')",
      "hint": "Press Run. Notice the function prints twice, once for each call.",
      "codeHeight": 6,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {
            "type": "solution_code",
            "solutionCode": "def greet(name):\n  print('Hi,', name)\n\ngreet('Ali')\ngreet('Sam')"
          }
        ]
      }
    },
    {
      "title": "Edit farewell",
      "content": "<p>Here's a function called <code>farewell</code>. It only says goodbye to Ali, and the message is a bit boring.</p>",
      "task": "Change the body of <code>farewell</code> so it prints <code>See you tomorrow,</code> followed by the name. Then add another call so it also says goodbye to <code>'Sam'</code>.",
      "starterCode": "def farewell(name):\n  print('Bye')\n\nfarewell('Ali')",
      "hint": "Inside the <code>print</code>, write <code>'See you tomorrow,'</code> then <code>, name</code> so Python adds the name after the message. Then on a new line below, write <code>farewell('Sam')</code>.",
      "codeHeight": 6,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def farewell(name):", "description": "Keep the def farewell(name): line"},
          {"type": "output_contains", "pattern": "See you tomorrow, Ali", "description": "Output should contain 'See you tomorrow, Ali'"},
          {"type": "output_contains", "pattern": "See you tomorrow, Sam", "description": "Output should contain 'See you tomorrow, Sam'"}
        ]
      }
    },
    {
      "type": "concept",
      "icon": "↩️",
      "title": "Print vs return: give back an answer",
      "content": "<p>Some Python tools <strong>do things</strong>. Others <strong>give back an answer</strong>:</p><ul><li><code>print(\"hi\")</code> shows <code>hi</code> on the screen. It doesn't give anything back.</li><li><code>len([1, 2, 3])</code> gives back <code>3</code>. You can save it: <code>n = len([1, 2, 3])</code>.</li></ul><p>Your functions can do the same. Use <code>return</code> instead of <code>print</code> to give an answer back:</p><pre style='background:#272822;color:#f8f8f2;padding:10px 14px;border-radius:6px;margin:8px 0;'><span style='color:#f92672'>def</span> <span style='color:#fd971f'>shout</span>(text):\n  <span style='color:#f92672'>return</span> text + <span style='color:#e6db74'>\"!\"</span>\n\nx = shout(<span style='color:#e6db74'>\"hi\"</span>)    <span style='color:#cccccc'># x is now \"hi!\"</span>\n<span style='color:#66d9ef'>print</span>(x)           <span style='color:#cccccc'># shows hi!</span></pre><p>The next few problems use a <strong>spec table</strong>. It calls your function with different inputs and shows what it gave back. Your job is to make every row pass.</p>",
      "footer": "print SHOWS the answer. return GIVES IT BACK so you can use it later."
    },
    {
      "title": "Meet the spec table",
      "content": "<p>Here's a function <code>loud</code> that takes some text and gives back a louder version. Look at the spec table below the editor: it shows three calls and what each one should give back.</p>",
      "task": "Run the code. The spec table should fill in green ticks for every row.",
      "starterCode": "def loud(text):\n  return text + \"!!!\"",
      "hint": "Just press Run. The spec table calls <code>loud(\"hi\")</code>, <code>loud(\"yay\")</code>, and <code>loud(\"oh\")</code> and checks each return value.",
      "codeHeight": 4,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {
            "type": "function_spec",
            "functionName": "loud",
            "cases": [
              {"args": ["hi"], "expected": "hi!!!"},
              {"args": ["yay"], "expected": "yay!!!"},
              {"args": ["oh"], "expected": "oh!!!"}
            ]
          }
        ]
      }
    },
    {
      "title": "Returning a number",
      "content": "<p>Functions can return numbers too, not just strings. Here's <code>double</code>: it gives back twice the number you pass in.</p>",
      "task": "Run the code and watch the spec table fill in.",
      "starterCode": "def double(n):\n  return n * 2",
      "hint": "Press Run. The spec table calls <code>double(2)</code>, <code>double(3)</code>, <code>double(7)</code> and checks each return value.",
      "codeHeight": 4,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {
            "type": "function_spec",
            "functionName": "double",
            "cases": [
              {"args": [2], "expected": 4},
              {"args": [3], "expected": 6},
              {"args": [7], "expected": 14}
            ]
          }
        ]
      }
    },
    {
      "title": "Turn double into square",
      "content": "<p>You've seen <code>double(n)</code> return <code>n * 2</code>. Now make a function called <code>square</code> that returns <code>n</code> times itself: <code>n * n</code>.</p>",
      "task": "Rename the function to <code>square</code> and change the body so the spec table goes green.",
      "starterCode": "def double(n):\n  return n * 2",
      "hint": "Two changes: give the function a new name, and update the body so it multiplies <code>n</code> by itself instead of by 2.",
      "codeHeight": 4,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {
            "type": "function_spec",
            "functionName": "square",
            "cases": [
              {"args": [3], "expected": 9},
              {"args": [5], "expected": 25},
              {"args": [7], "expected": 49},
              {"args": [10], "expected": 100}
            ]
          }
        ]
      }
    },
    {
      "title": "Two parameters",
      "content": "<p>A function can take more than one input. Just list them inside the brackets, separated by a comma:</p><pre style='background:#f1f5f9;padding:8px 12px;border-radius:4px;margin:8px 0;'>def combine(first, last):\n  return first + \" \" + last</pre><p>Now <code>combine</code> needs <em>two</em> values when you call it: <code>combine(\"Sam\", \"Lee\")</code>.</p>",
      "task": "Run the code and watch the spec table check two-parameter calls.",
      "starterCode": "def combine(first, last):\n  return first + \" \" + last",
      "hint": "Press Run. The spec table calls <code>combine(\"Sam\", \"Lee\")</code>, <code>combine(\"Pat\", \"Kim\")</code>, etc.",
      "codeHeight": 4,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {
            "type": "function_spec",
            "functionName": "combine",
            "cases": [
              {"args": ["Sam", "Lee"], "expected": "Sam Lee"},
              {"args": ["Pat", "Kim"], "expected": "Pat Kim"},
              {"args": ["Ali", "Smith"], "expected": "Ali Smith"}
            ]
          }
        ]
      }
    },
    {
      "title": "Roll-call format",
      "content": "<p>School roll-calls usually go <em>last name, first name</em>, like <code>\"Lee, Sam\"</code>.</p>",
      "task": "Change the body of <code>combine</code> so it returns the last name first, then a comma and a space, then the first name.",
      "starterCode": "def combine(first, last):\n  return first + \" \" + last",
      "hint": "Put <code>last</code> before <code>first</code> in the return. The separator in the middle is a comma followed by a space, instead of just a space.",
      "codeHeight": 4,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {
            "type": "function_spec",
            "functionName": "combine",
            "cases": [
              {"args": ["Sam", "Lee"], "expected": "Lee, Sam"},
              {"args": ["Pat", "Kim"], "expected": "Kim, Pat"},
              {"args": ["Ali", "Smith"], "expected": "Smith, Ali"}
            ]
          }
        ]
      }
    },
    {
      "title": "Don't forget to return",
      "content": "<p>This function <em>computes</em> the right answer but it never gives it back. The spec table shows <code>None</code> for every row. That's what Python uses when a function returns nothing.</p>",
      "task": "Add a <code>return</code> so the spec passes.",
      "starterCode": "def triple(n):\n  result = n * 3\n  # this function forgot to return",
      "hint": "Add one more line at the same indent as <code>result = ...</code>. Use <code>return</code> to give <code>result</code> back to whoever called the function.",
      "codeHeight": 5,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "return", "description": "Add a return statement"},
          {
            "type": "function_spec",
            "functionName": "triple",
            "cases": [
              {"args": [2], "expected": 6},
              {"args": [4], "expected": 12},
              {"args": [10], "expected": 30}
            ]
          }
        ]
      }
    },
    {
      "title": "Write area from scratch",
      "content": "<p>Time to write your own. The <code>area</code> function takes a width and a height and gives back the area of the rectangle (width times height).</p>",
      "task": "Write a function called <code>area</code> with two parameters: <code>w</code> and <code>h</code>. Return <code>w * h</code>.",
      "starterCode": "# Write your area function below\n",
      "hint": "Your <code>def</code> line lists two parameters in the brackets. The body uses <code>return</code> with width times height.",
      "codeHeight": 5,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def area", "description": "Define a function called area"},
          {"type": "code_contains", "pattern": "return", "description": "Use return to give back the answer"},
          {
            "type": "function_spec",
            "functionName": "area",
            "cases": [
              {"args": [3, 4], "expected": 12},
              {"args": [5, 5], "expected": 25},
              {"args": [2, 7], "expected": 14},
              {"args": [10, 1], "expected": 10}
            ]
          }
        ]
      }
    },
    {
      "type": "concept",
      "icon": "🧩",
      "title": "Tools using other tools",
      "content": "<p>Once a function is defined, your <em>other</em> functions can use it too. That's how big programs get built: small tools combine into bigger ones.</p><pre style='background:#272822;color:#f8f8f2;padding:10px 14px;border-radius:6px;margin:8px 0;'><span style='color:#f92672'>def</span> <span style='color:#fd971f'>shout</span>(text):\n  <span style='color:#f92672'>return</span> text + <span style='color:#e6db74'>\"!!!\"</span>\n\n<span style='color:#f92672'>def</span> <span style='color:#fd971f'>double_shout</span>(text):\n  <span style='color:#f92672'>return</span> shout(text) + <span style='color:#e6db74'>\" \"</span> + shout(text)\n\n<span style='color:#66d9ef'>print</span>(double_shout(<span style='color:#e6db74'>\"yay\"</span>))   <span style='color:#cccccc'># yay!!! yay!!!</span></pre><p>The next few problems give you a row of <strong>buttons</strong> next to your code. Each button calls your function with different inputs, so you don't have to type the calls yourself. Just write the function, click Run, then click the buttons to try it out.</p>",
      "footer": "Combine small tools to build big ones."
    },
    {
      "title": "Watch stars run",
      "content": "<p>This function prints <code>n</code> stars on a line. The trick is that <code>\"*\" * n</code> repeats the star <code>n</code> times. Python's <code>*</code> operator can repeat strings, not just multiply numbers.</p>",
      "task": "Click Run, then click each button below to see <code>stars</code> called with different values.",
      "starterCode": "def stars(n):\n  print(\"*\" * n)",
      "hint": "Press Run first to define the function. Then try the buttons: each one calls <code>stars</code> with a different number.",
      "codeHeight": 4,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {
            "type": "function_buttons",
            "calls": ["stars(3)", "stars(7)", "stars(12)"],
            "solutionCode": "def stars(n):\n  print(\"*\" * n)"
          }
        ]
      }
    },
    {
      "title": "Make a row",
      "content": "<p><code>stars</code> only prints stars. Let's generalise it: a <code>row</code> function takes a <em>character</em> and a <em>number</em>, and prints that character repeated.</p>",
      "task": "Write <code>row(char, n)</code> so it prints <code>char</code> repeated <code>n</code> times. Then click the buttons to try it.",
      "starterCode": "# Write row(char, n) below\n",
      "hint": "Two parameters in the brackets. Inside, <code>print</code> the character multiplied by the number (the <code>*</code> operator repeats strings).",
      "codeHeight": 4,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def row", "description": "Define a function called row"},
          {
            "type": "function_buttons",
            "calls": ["row('*', 5)", "row('#', 8)", "row('@', 3)"],
            "solutionCode": "def row(char, n):\n  print(char * n)"
          }
        ]
      }
    },
    {
      "title": "Build a box",
      "content": "<p>A box is just a row, repeated. The <code>box</code> function should print an <code>n</code>-by-<code>n</code> square of stars by calling <code>row</code> in a loop.</p><p><code>row</code> is already defined for you. Your <code>box</code> function should <em>use</em> it.</p>",
      "task": "Write <code>box(n)</code> that calls <code>row(\"*\", n)</code> a total of <code>n</code> times. Click Run, then try the buttons.",
      "starterCode": "def row(char, n):\n  print(char * n)\n\ndef box(n):\n  # call row n times\n",
      "hint": "Inside <code>box</code>, use a <code>for</code> loop that runs <code>n</code> times. Each time through, call <code>row</code> to print one line of <code>n</code> stars.",
      "codeHeight": 8,
      "difficulty": "hard",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def box", "description": "Define a function called box"},
          {"type": "code_contains", "pattern": "row(", "description": "Your box function should call row()"},
          {
            "type": "function_buttons",
            "calls": ["box(3)", "box(5)", "box(7)"],
            "solutionCode": "def row(char, n):\n  print(char * n)\n\ndef box(n):\n  for i in range(n):\n    row(\"*\", n)"
          }
        ]
      }
    },
    {
      "title": "Triangle",
      "content": "<p>A triangle starts with a row of 1 star, then 2 stars, then 3, all the way up to <code>n</code>. You can build it by calling <code>row</code> with different lengths inside a loop.</p>",
      "task": "Write <code>triangle(n)</code> that prints rows of stars, growing from 1 up to <code>n</code>.",
      "starterCode": "def row(char, n):\n  print(char * n)\n\ndef triangle(n):\n  # print rows of length 1, 2, 3, ... up to n\n",
      "hint": "Use <code>range</code> with a start and end so <code>i</code> goes 1, 2, 3, ..., up to <code>n</code>. Inside the loop, call <code>row</code> with <code>i</code> stars.",
      "codeHeight": 8,
      "difficulty": "hard",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def triangle", "description": "Define a function called triangle"},
          {"type": "code_contains", "pattern": "row(", "description": "Your triangle function should call row()"},
          {
            "type": "function_buttons",
            "calls": ["triangle(4)", "triangle(7)"],
            "solutionCode": "def row(char, n):\n  print(char * n)\n\ndef triangle(n):\n  for i in range(1, n+1):\n    row(\"*\", i)"
          }
        ]
      }
    },
    {
      "title": "Design your own shape",
      "content": "<p>Time to invent your own. Write a function that prints any shape you like: a tall pillar, a diamond, your initial, anything.</p><p>You can use <code>row</code> inside it, or just <code>print</code> directly. Whatever you make, give it a name and call it at the bottom so it shows in the output.</p>",
      "task": "Write your own function that prints a shape, then call it.",
      "starterCode": "def row(char, n):\n  print(char * n)\n\n# Define your own function below, then call it\n",
      "hint": "Try something like a <code>pillar</code> function with a loop that calls <code>row</code> with a single character. Or get creative.",
      "codeHeight": 10,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def ", "description": "Define your own function"},
          {"type": "output_not_empty", "description": "Call your function so it prints something"}
        ]
      }
    },
    {
      "title": "Total of a list",
      "content": "<p>Let's combine functions with what you learned in Worksheet 6. Write a function called <code>total</code> that takes a list of prices and returns their sum.</p>",
      "task": "Write <code>total(prices)</code> that adds up all the prices in the list and returns the total.",
      "starterCode": "# Write total(prices) below\n",
      "hint": "Start a running total at 0. Loop through each price in the list and add it to the running total. After the loop, <code>return</code> the total.",
      "codeHeight": 8,
      "difficulty": "hard",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def total", "description": "Define a function called total"},
          {"type": "code_contains", "pattern": "return", "description": "Use return to give back the total"},
          {
            "type": "function_spec",
            "functionName": "total",
            "cases": [
              {"args": [[2, 3, 5]], "expected": 10},
              {"args": [[10, 20, 30, 40]], "expected": 100},
              {"args": [[7]], "expected": 7},
              {"args": [[]], "expected": 0}
            ]
          }
        ]
      }
    }
  ],
  "metadata": {
    "version": "1.0",
    "created": "2026-04-30"
  }
}
