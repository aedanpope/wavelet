{
  "id": "worksheet-7",
  "title": "Worksheet 7: Functions",
  "description": "Make your own Python tools using def, parameters, and return. Then put them together.",
  "problems": [
    {
      "type": "concept",
      "icon": "🔧",
      "title": "Writing your own functions",
      "content": "<p>You've already used Python's built-in <strong>functions</strong>, tools like <code>print()</code>, <code>len()</code>, and <code>range()</code> that come with Python.<br>The keyword <b>def</b> (short for <em>define</em>) lets you build your own:</p><pre style='background:#272822;color:#f8f8f2;padding:10px 14px;border-radius:6px;margin:8px 0;'><span style='color:#f92672'>def</span> <span style='color:#fd971f'>say_hi</span>():           <span style='color:#cccccc'>&larr; define</span>\n  <span style='color:#66d9ef'>print</span>(<span style='color:#e6db74'>\"Hi there!\"</span>)    <span style='color:#cccccc'>&larr; specify</span>\n\nsay_hi()                <span style='color:#cccccc'>&larr; call, now \"Hi there!\" gets printed</span></pre><p>The function specification must be <strong>indented</strong>, so Python knows which lines belong to the function. Nothing happens until you <em>call</em> it.</p>",
      "footer": "A function is a named piece of code you can call whenever you want."
    },
    {
      "type": "trace",
      "title": "Watch a function being called",
      "code": "def say_hi():\n  print(\"Hi there!\")\n\nsay_hi()",
      "steps": [
        {"line": 1, "locals": {}, "for_ctx": null, "phase": "before", "ann": null, "output": ""},
        {"line": 1, "locals": {"say_hi": "<function>"}, "for_ctx": null, "phase": "after", "ann": null, "output": ""},
        {"line": 4, "locals": {"say_hi": "<function>"}, "for_ctx": null, "phase": "before", "ann": null, "output": ""},
        {"line": 2, "locals": {"say_hi": "<function>"}, "for_ctx": null, "phase": "before", "ann": {"type": "print", "preview": "Hi there!"}, "output": ""},
        {"line": 2, "locals": {"say_hi": "<function>"}, "for_ctx": null, "phase": "after", "ann": null, "output": "Hi there!\n"},
        {"line": 4, "locals": {"say_hi": "<function>"}, "for_ctx": null, "phase": "after", "ann": null, "output": "Hi there!\n"}
      ]
    },
    {
      "title": "Call it more than once",
      "content": "<p>Once a function is defined, you can call it as many times as you like. Each call runs the body again.</p>",
      "task": "Add two more <code>say_hi()</code> calls so the message prints three times in total.",
      "starterCode": "def say_hi():\n  print(\"Hi there!\")\n\nsay_hi()\n",
      "hint": "Just write <code>say_hi()</code> on two new lines below.",
      "codeHeight": 6,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def say_hi", "description": "Keep the def say_hi() line"},
          {"type": "ast_has_function_call", "function": "say_hi", "minCount": 3, "description": "Call say_hi() three times"},
          {"type": "solution_code", "solutionCode": "def say_hi():\n  print(\"Hi there!\")\n\nsay_hi()\nsay_hi()\nsay_hi()"}
        ]
      }
    },
    {
      "title": "Write your own function",
      "content": "<p>Your turn. Make a function called <code>cheer</code> that prints <code>\"Hooray!\"</code>, then call it.</p>",
      "task": "Define <code>cheer</code> and call it once.",
      "starterCode": "# Define cheer here\n\n\n# Call it here\n",
      "hint": "Start with a <code>def</code> line for <code>cheer</code>. Indent a <code>print</code> of <code>\"Hooray!\"</code> on the next line. After your function, add a line that calls it.",
      "codeHeight": 6,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def cheer", "description": "Define a function called cheer"},
          {"type": "ast_has_function_call", "function": "cheer", "description": "Call your function by writing cheer() on its own line"},
          {"type": "solution_code", "solutionCode": "def cheer():\n  print(\"Hooray!\")\n\ncheer()"}
        ]
      }
    },
    {
      "type": "concept",
      "icon": "📥",
      "title": "Parameters: your tool takes an input",
      "content": "<p>Your functions can take inputs too, the input goes in the brackets after the function name:</p><pre style='background:#272822;color:#f8f8f2;padding:10px 14px;border-radius:6px;margin:8px 0;'><span style='color:#f92672'>def</span> <span style='color:#fd971f'>greet</span>(name):    <span style='color:#cccccc'>&larr; add a parameter called 'name' to the function definition</span>\n  <span style='color:#66d9ef'>print</span>(<span style='color:#e6db74'>'Hi,'</span>, name)\n\ngreet(<span style='color:#e6db74'>'Ali'</span>)    <span style='color:#cccccc'># Hi, Ali</span>\ngreet(<span style='color:#e6db74'>'Sam'</span>)    <span style='color:#cccccc'># Hi, Sam</span></pre><p>When you call the function, the value you pass becomes that parameter inside the function.</p>",
      "footer": "Same function. Different inputs. Different output."
    },
    {
      "title": "Greet two friends",
      "content": "<p>Look at how <code>greet</code> uses the parameter <code>name</code>. Each call sends a different value in.</p>",
      "task": "Run the code and watch how the same function produces two different greetings.",
      "starterCode": "def greet(name):\n  print('Hi,', name)\n\ngreet('Ali')\ngreet('Sam')",
      "hint": "Press Run. Notice the function prints twice, once for each call.",
      "codeHeight": 6,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {
            "type": "solution_code",
            "solutionCode": "def greet(name):\n  print('Hi,', name)\n\ngreet('Ali')\ngreet('Sam')"
          }
        ]
      }
    },
    {
      "title": "Edit farewell",
      "content": "<p>Here's a function called <code>farewell</code>. It only says goodbye to Ali, and the message is a bit boring.</p>",
      "task": "Change the body of <code>farewell</code> so it prints <code>See you tomorrow</code> followed by the name. Then add another call so it also says goodbye to <code>'Sam'</code>.",
      "starterCode": "def farewell(name):\n  print('Bye')\n\nfarewell('Ali')",
      "hint": "Inside the <code>print</code>, write <code>'See you tomorrow'</code> then <code>, name</code> so Python prints the name after the message (with a space between). Then on a new line below, write <code>farewell('Sam')</code>.",
      "codeHeight": 6,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def farewell(name):", "description": "Keep the def farewell(name): line"},
          {"type": "ast_has_function_call", "function": "farewell", "minCount": 2, "description": "Call farewell() twice (once for Ali, once for Sam)"},
          {"type": "solution_code", "solutionCode": "def farewell(name):\n  print('See you tomorrow', name)\n\nfarewell('Ali')\nfarewell('Sam')"}
        ]
      }
    },
    {
      "type": "concept",
      "icon": "↩️",
      "title": "Print vs return: give back an answer",
      "content": "<p>Some Python tools <strong>do things</strong>. Others <strong>give back an answer</strong>:</p><ul><li><code>print(\"hi\")</code> shows <code>hi</code> on the screen. It doesn't give anything back.</li><li><code>len([1, 2, 3])</code> gives back <code>3</code>.</li></ul><p>Use <code><strong>return</strong></code> instead of <code><strong>print</strong></code> to give an answer back:</p><pre style='background:#272822;color:#f8f8f2;padding:10px 14px;border-radius:6px;margin:8px 0;'><span style='color:#f92672'>def</span> <span style='color:#fd971f'>shout</span>(text):\n  <span style='color:#f92672'>return</span> text + <span style='color:#e6db74'>\"!\"</span>    <span style='color:#cccccc'>&larr; gives back the answer</span>\n\nx = shout(<span style='color:#e6db74'>\"hi\"</span>)\n<span style='color:#66d9ef'>print</span>(x)    <span style='color:#cccccc'># shows hi!</span></pre>",
      "footer": "<code><strong>print</strong></code> shows the answer. <code><strong>return</strong></code> gives it back so you can use it later."
    },
    {
      "title": "Double it",
      "content": "<p>Time to write your first <code>return</code>. The <code>double</code> function takes a number and gives back twice that number.</p>",
      "task": "Finish the <code>double</code> function by writing its <code>return</code> line. Then call it twice to print the double of 5 and then of 12, using two lines of <code>print(double(...))</code>.",
      "starterCode": "def double(n):\n  # Write a return statement here\n  \n\n# Now call double() twice to print double of 5 and double of 12\n",
      "hint": "After <code>return</code>, write <code>n * 2</code>. Then below, use <code>print(double(5))</code> and <code>print(double(12))</code> on two new lines.",
      "codeHeight": 8,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def double(n):", "description": "Keep the def double(n): line"},
          {"type": "code_contains", "pattern": "return", "description": "Use return to give back the answer"},
          {"type": "ast_has_function_call", "function": "double", "minCount": 2, "description": "Call double() twice (once for 5, once for 12)"},
          {"type": "solution_code", "solutionCode": "def double(n):\n  return n * 2\n\nprint(double(5))\nprint(double(12))"}
        ]
      }
    },
    {
      "title": "Meet the spec table",
      "content": "<p>You just wrote <code>double</code> by hand. The <strong>spec table</strong> below the editor calls your function with different inputs and checks what it gave back. Your job is to make every row pass.</p>",
      "task": "The spec table below is failing every row, because the function is returning the number unchanged instead of doubling it. Fix the body of <code>double</code> so all three rows turn green.",
      "starterCode": "def double(n):\n  return n * 1",
      "hint": "Look at the first row: <code>double(2)</code> should give back <code>4</code>, but it's giving back <code>2</code>. Change <code>n * 1</code> to <code>n * 2</code>.",
      "codeHeight": 4,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {
            "type": "function_spec",
            "functionName": "double",
            "cases": [
              {"args": [2], "expected": 4},
              {"args": [7], "expected": 14},
              {"args": [50], "expected": 100}
            ]
          }
        ]
      }
    },
    {
      "title": "Turn double into square",
      "content": "<p>You've seen <code>double(n)</code> return <code>n * 2</code>. Now make a function called <code>square</code> that returns <code>n</code> times itself: <code>n * n</code>.</p>",
      "task": "Rename the function to <code>square</code> and change the body so the spec table goes green.",
      "starterCode": "def double(n):\n  return n * 2",
      "hint": "Two changes: give the function a new name, and update the body so it multiplies <code>n</code> by itself instead of by 2.",
      "codeHeight": 4,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {
            "type": "function_spec",
            "functionName": "square",
            "cases": [
              {"args": [3], "expected": 9},
              {"args": [5], "expected": 25},
              {"args": [7], "expected": 49},
              {"args": [10], "expected": 100}
            ]
          }
        ]
      }
    },
    {
      "title": "Two parameters",
      "content": "<p>A function can take more than one input. Just list them inside the brackets, separated by a comma:</p><pre style='background:#f1f5f9;padding:8px 12px;border-radius:4px;margin:8px 0;'>def combine(first, last):\n  return first + \" \" + last</pre><p>Now <code>combine</code> needs <em>two</em> values when you call it: <code>combine(\"Sam\", \"Lee\")</code>.</p>",
      "task": "Run the code and watch the spec table check two-parameter calls.",
      "starterCode": "def combine(first, last):\n  return first + \" \" + last",
      "hint": "Press Run. The spec table calls <code>combine(\"Sam\", \"Lee\")</code>, <code>combine(\"Pat\", \"Kim\")</code>, etc.",
      "codeHeight": 4,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {
            "type": "function_spec",
            "functionName": "combine",
            "cases": [
              {"args": ["Sam", "Lee"], "expected": "Sam Lee"},
              {"args": ["Pat", "Kim"], "expected": "Pat Kim"},
              {"args": ["Ali", "Smith"], "expected": "Ali Smith"}
            ]
          }
        ]
      }
    },
    {
      "title": "Song by artist",
      "content": "<p>When we talk about songs, we usually say the title first and then the artist, like <code>\"Anti-Hero by Taylor\"</code>.</p>",
      "task": "Change the body of <code>song</code> so it returns the title first, then <code>\" by \"</code>, then the artist.",
      "starterCode": "def song(artist, title):\n  return artist + \" \" + title",
      "hint": "Put <code>title</code> before <code>artist</code> in the return. The separator in the middle is <code>\" by \"</code> (with a space on each side), instead of just <code>\" \"</code>.",
      "codeHeight": 4,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {
            "type": "function_spec",
            "functionName": "song",
            "cases": [
              {"args": ["Taylor", "Anti-Hero"], "expected": "Anti-Hero by Taylor"},
              {"args": ["Adele", "Hello"], "expected": "Hello by Adele"},
              {"args": ["Beyonce", "Halo"], "expected": "Halo by Beyonce"}
            ]
          }
        ]
      }
    },
    {
      "title": "Write area from scratch",
      "content": "<p>Time to write your own. The <code>area</code> function takes a width and a height and gives back the area of the rectangle (width times height).</p>",
      "task": "Write a function called <code>area</code> with two parameters: <code>w</code> and <code>h</code>. Return <code>w * h</code>.",
      "starterCode": "# Write your area function below\n",
      "hint": "Your <code>def</code> line lists two parameters in the brackets. The body uses <code>return</code> with width times height.",
      "codeHeight": 5,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def area", "description": "Define a function called area"},
          {"type": "code_contains", "pattern": "return", "description": "Use return to give back the answer"},
          {
            "type": "function_spec",
            "functionName": "area",
            "cases": [
              {"args": [3, 4], "expected": 12},
              {"args": [5, 5], "expected": 25},
              {"args": [2, 7], "expected": 14},
              {"args": [10, 1], "expected": 10}
            ]
          }
        ]
      }
    },
    {
      "type": "concept",
      "icon": "🧩",
      "title": "Tools using other tools",
      "content": "<p>Once a function is defined, your <em>other</em> functions can use it too. That's how big programs get built: small tools combine into bigger ones.</p><pre style='background:#272822;color:#f8f8f2;padding:10px 14px;border-radius:6px;margin:8px 0;'><span style='color:#f92672'>def</span> <span style='color:#fd971f'>shout</span>(text):\n  <span style='color:#f92672'>return</span> text + <span style='color:#e6db74'>\"!!!\"</span>\n\n<span style='color:#f92672'>def</span> <span style='color:#fd971f'>double_shout</span>(text):\n  <span style='color:#f92672'>return</span> shout(text) + <span style='color:#e6db74'>\" \"</span> + shout(text)\n\n<span style='color:#66d9ef'>print</span>(double_shout(<span style='color:#e6db74'>\"yay\"</span>))   <span style='color:#cccccc'># yay!!! yay!!!</span></pre>",
      "footer": "Combine small tools to build big ones."
    },
    {
      "title": "Watch stars run",
      "content": "<p>This function prints <code>n</code> stars on a line. The trick is that <code>\"*\" * n</code> repeats the star <code>n</code> times. Python's <code>*</code> operator can repeat strings, not just multiply numbers.</p><p>The starter calls <code>stars</code> three times with different numbers, so you can see the output for each.</p>",
      "task": "Run the code and watch <code>stars</code> get called with three different values.",
      "starterCode": "def stars(n):\n  print(\"*\" * n)\n\nstars(3)\nstars(5)\nstars(8)",
      "hint": "Just press Run. The function prints once for each call, so you should see three lines of stars.",
      "codeHeight": 7,
      "difficulty": "easy",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def stars(n):", "description": "Keep the def stars(n): line"},
          {"type": "solution_code", "solutionCode": "def stars(n):\n  print(\"*\" * n)\n\nstars(3)\nstars(5)\nstars(8)"}
        ]
      }
    },
    {
      "title": "Build a box",
      "content": "<p>A box is just a row of stars, repeated. The <code>box</code> function should print an <code>n</code>-by-<code>n</code> square by calling <code>stars(n)</code> a total of <code>n</code> times.</p><p><code>stars</code> is already defined for you. Your <code>box</code> function should <em>use</em> it.</p>",
      "task": "Write <code>box(n)</code> so it calls <code>stars(n)</code> a total of <code>n</code> times. The starter already calls <code>box(3)</code> and <code>box(5)</code> at the bottom.",
      "starterCode": "def stars(n):\n  print(\"*\" * n)\n\ndef box(n):\n  # call stars(n) n times\n  \n\nbox(3)\nbox(5)",
      "hint": "Inside <code>box</code>, use a <code>for</code> loop that runs <code>n</code> times. Each time through, call <code>stars(n)</code> to print one line.",
      "codeHeight": 10,
      "difficulty": "hard",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def box", "description": "Define a function called box"},
          {"type": "ast_has_function_call", "function": "stars", "description": "Your box function should call stars()"},
          {"type": "solution_code", "solutionCode": "def stars(n):\n  print(\"*\" * n)\n\ndef box(n):\n  for i in range(n):\n    stars(n)\n\nbox(3)\nbox(5)"}
        ]
      }
    },
    {
      "title": "Triangle",
      "content": "<p>A triangle starts with a row of 1 star, then 2 stars, then 3, all the way up to <code>n</code>. You can build it by calling <code>stars</code> with different lengths inside a loop.</p>",
      "task": "Write <code>triangle(n)</code> so it prints rows of stars, growing from 1 up to <code>n</code>. The starter already calls <code>triangle(4)</code> and <code>triangle(7)</code> at the bottom.",
      "starterCode": "def stars(n):\n  print(\"*\" * n)\n\ndef triangle(n):\n  # print rows of length 1, 2, 3, ... up to n\n  \n\ntriangle(4)\ntriangle(7)",
      "hint": "Use <code>range</code> with a start and end so <code>i</code> goes 1, 2, 3, ..., up to <code>n</code>. Inside the loop, call <code>stars(i)</code>.",
      "codeHeight": 10,
      "difficulty": "hard",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def triangle", "description": "Define a function called triangle"},
          {"type": "ast_has_function_call", "function": "stars", "description": "Your triangle function should call stars()"},
          {"type": "solution_code", "solutionCode": "def stars(n):\n  print(\"*\" * n)\n\ndef triangle(n):\n  for i in range(1, n+1):\n    stars(i)\n\ntriangle(4)\ntriangle(7)"}
        ]
      }
    },
    {
      "title": "Design your own shape",
      "content": "<p>Time to invent your own. Write a function that prints any shape you like: a tall pillar, a diamond, your initial, anything.</p><p>You can use <code>stars</code> inside it, or just <code>print</code> directly. Whatever you make, give it a name and call it at the bottom so it shows in the output.</p>",
      "task": "Write your own function that prints a shape, then call it.",
      "starterCode": "def stars(n):\n  print(\"*\" * n)\n\n# Define your own function below, then call it\n",
      "hint": "Try something like a <code>pillar</code> function with a loop that calls <code>stars(1)</code>. Or get creative.",
      "codeHeight": 10,
      "difficulty": "medium",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def ", "description": "Define your own function"},
          {"type": "output_not_empty", "description": "Call your function so it prints something"}
        ]
      }
    },
    {
      "title": "Total of a list",
      "content": "<p>Let's combine functions with what you learned in Worksheet 6. Write a function called <code>total</code> that takes a list of prices and returns their sum.</p>",
      "task": "Write <code>total(prices)</code> that adds up all the prices in the list and returns the total.",
      "starterCode": "# Write total(prices) below\n",
      "hint": "Start a running total at 0. Loop through each price in the list and add it to the running total. After the loop, <code>return</code> the total.",
      "codeHeight": 8,
      "difficulty": "hard",
      "validation": {
        "rules": [
          {"type": "code_contains", "pattern": "def total", "description": "Define a function called total"},
          {"type": "code_contains", "pattern": "return", "description": "Use return to give back the total"},
          {
            "type": "function_spec",
            "functionName": "total",
            "cases": [
              {"args": [[2, 3, 5]], "expected": 10},
              {"args": [[10, 20, 30, 40]], "expected": 100},
              {"args": [[7]], "expected": 7},
              {"args": [[]], "expected": 0}
            ]
          }
        ]
      }
    }
  ],
  "metadata": {
    "version": "1.0",
    "created": "2026-04-30"
  }
}
