{
  "id": "pixel-game",
  "title": "Pixel Game",
  "description": "Build a tiny scene that comes alive when you press the arrow buttons.",
  "intro": "<p>Build your own pixel-game scene that <strong>animates when you press the arrow buttons</strong>. You'll fill in a few small functions, then click <em>Run Project</em> to see them all run together.</p><p class=\"laptop-only-note\">💻 <strong>This project works best on a laptop.</strong> You'll be using the keyboard a lot.</p>",
  "canvas": "GRID20",
  "lockedPreamble": [
    "use_canvas(GRID20)",
    "from types import SimpleNamespace"
  ],
  "setupSeed": "state = SimpleNamespace()\n",
  "editablePreamble": "state.player_x = 10\nstate.player_y = 10\n# Add your own state variables below, e.g.\n# state.score = 0\n# state.colour = 'red'\n",
  "tasks": [
    {
      "type": "concept",
      "icon": "📍",
      "title": "Pixel coordinates",
      "content": "<p>Use the <code>draw(x, y, colour)</code> function to colour in cells on the canvas.</p><p>Every cell has an address written as <code>(x, y)</code>:</p><ul><li><strong>x</strong> goes across (0 on the left, 19 on the right).</li><li><strong>y</strong> goes up (0 at the bottom, 19 at the top).</li></ul><div style='display:flex;flex-wrap:wrap;align-items:center;justify-content:center;gap:14px;margin:14px 0;'><code style='font-size:1rem;'>draw(1, 0, 'red')</code><span style='font-size:1.6rem;color:#92400e;'>&rarr;</span><table style='border-collapse:collapse;font-family:Consolas,Monaco,monospace;font-size:0.95rem;'><tr><td style='border:1px solid #d1d5db;padding:10px 14px;background:#f9fafb;text-align:center;'>(0, 2)</td><td style='border:1px solid #d1d5db;padding:10px 14px;background:#f9fafb;text-align:center;'>(1, 2)</td><td style='border:1px solid #d1d5db;padding:10px 14px;background:#f9fafb;text-align:center;'>(2, 2)</td></tr><tr><td style='border:1px solid #d1d5db;padding:10px 14px;background:#f9fafb;text-align:center;'>(0, 1)</td><td style='border:1px solid #d1d5db;padding:10px 14px;background:#f9fafb;text-align:center;'>(1, 1)</td><td style='border:1px solid #d1d5db;padding:10px 14px;background:#f9fafb;text-align:center;'>(2, 1)</td></tr><tr><td style='border:1px solid #d1d5db;padding:10px 14px;background:#f9fafb;text-align:center;'>(0, 0)</td><td style='border:1px solid #d1d5db;padding:10px 14px;background:#dc2626;color:white;text-align:center;font-weight:600;'>(1, 0)</td><td style='border:1px solid #d1d5db;padding:10px 14px;background:#f9fafb;text-align:center;'>(2, 0)</td></tr></table></div><p><strong>Colour</strong> is a string (in quotes). Try <code>'red'</code>, <code>'blue'</code>, <code>'gold'</code>, <code>'hotpink'</code>, <code>'tomato'</code>, or <code>'aquamarine'</code>. <a href='https://www.w3schools.com/colors/colors_names.asp' target='_blank' rel='noopener'>See the full list of colour names</a>.</p>",
      "footer": "x across, y up. (0, 0) is bottom-left."
    },
    {
      "id": "corners",
      "tier": "D",
      "title": "Colour the corners",
      "function": "draw_corners",
      "areaDescription": "Called from draw_scene. Paint the four corner cells of the canvas.",
      "guidance": "<p>Warm-up: draw a coloured cell in each of the four corners of the canvas.</p><p>The corners are at <code>(0, 0)</code>, <code>(19, 0)</code>, <code>(0, 19)</code>, and <code>(19, 19)</code>. Use <code>draw(x, y, 'colour')</code>, pick whatever colours you like.</p>",
      "starterBody": "draw(0, 0, 'red')\n# add the other three corners\n\n",
      "editorHeight": 6,
      "validation": {
        "rules": [
          {
            "type": "function_fills_cells",
            "cells": [[0, 0], [19, 0], [0, 19], [19, 19]],
            "exact": true,
            "anyColor": true
          }
        ]
      }
    },
    {
      "id": "border",
      "tier": "D",
      "title": "Draw a border",
      "function": "draw_border",
      "areaDescription": "Called from draw_scene. Paint the 76 edge cells of the canvas.",
      "guidance": "<p>A border goes all the way around the edge of the canvas. There are 76 cells in the border, way too many to type one by one. Use <code>for</code> loops instead.</p><p>Tip: you'll need <em>four</em> loops: bottom row, top row, left column, right column. Or one loop with some clever maths!</p>",
      "starterBody": "for x in range(20):\n  draw(x, 0, 'blue')\n# now do the top row, left column, and right column\n\n",
      "editorHeight": 10,
      "validation": {
        "rules": [
          {
            "type": "function_fills_cells",
            "cells": "border",
            "exact": true,
            "anyColor": true
          }
        ]
      }
    },
    {
      "id": "background",
      "tier": "A-B",
      "selfCheck": true,
      "title": "Draw the background",
      "function": "draw_background",
      "areaDescription": "Called from draw_scene first, so the player sits on top. Free creative space.",
      "guidance": "<p>Time to make the canvas your own. <code>draw_background()</code> is your blank slate: whatever you draw here sits behind the player.</p><p>Start by calling the two functions you've already written (<code>draw_border()</code> and <code>draw_corners()</code>), then add anything else you can think of. A few ideas to get you started:</p><ul><li>A row of coloured rectangles along the bottom for grass or sand</li><li>Rainbow stripes across the middle</li><li>A pattern of dots</li><li>A shape using nested loops, like a triangle, diamond, or letter</li><li>Anything else you can imagine</li></ul><p>Tick the box on this card when you're happy with your background.</p>",
      "starterBody": "draw_border()\ndraw_corners()\n# add your own stuff! rectangles, stripes, shapes, anything.\n\n",
      "editorHeight": 10,
      "validation": {
        "rules": [
          { "type": "function_runs_clean" }
        ]
      }
    },
    { "type": "setup-anchor" },
    {
      "id": "player",
      "tier": "C",
      "title": "Draw the player",
      "function": "draw_player",
      "areaDescription": "Called from draw_scene after draw_background, so the player sits on top.",
      "guidance": "<p>The player lives at <code>state.player_x</code>, <code>state.player_y</code>. The State section starts these at <code>(10, 10)</code>.</p><p>The starter copies those into local <code>x</code> and <code>y</code> so you can tweak them, then draws a single yellow cell. Want a bigger character? Add more <code>draw</code> calls for cells around the centre, like <code>draw(x + 1, y, 'yellow')</code> or <code>draw(x, y + 1, 'red')</code>.</p>",
      "starterBody": "x = state.player_x\ny = state.player_y\ndraw(x, y, 'yellow')\n",
      "editorHeight": 8,
      "validation": {
        "rules": [
          { "type": "function_runs_clean" }
        ]
      }
    },
    {
      "id": "scene",
      "tier": "C",
      "title": "Build the scene",
      "function": "draw_scene",
      "areaDescription": "Runs once on Run, and again after every arrow press. Call your other functions in order.",
      "guidance": "<p>This function runs every time you press a button. Its job is to put the whole picture together by calling your other functions.</p><p>Order matters: later draws appear on top of earlier ones. Background first, player last.</p>",
      "starterBody": "draw_background()\ndraw_player()\n",
      "editorHeight": 6,
      "validation": {
        "rules": [
          { "type": "function_runs_clean" }
        ]
      }
    },
    {
      "id": "buttons",
      "tier": "D",
      "title": "Make the buttons move the player",
      "areaDescription": "Each handler runs when its arrow button is clicked. draw_scene re-runs automatically afterwards, so the player visibly moves.",
      "guidance": "<p>The four arrow buttons each call one of these handler functions. <code>on_left_key</code> is already filled in for you, with <code>state.player_x -= 1</code> (subtract one from the player's <code>x</code> position).</p><p>Fill in the other three so each button moves the player one cell:</p><ul><li><code>on_right_key</code>: <em>add</em> one to <code>state.player_x</code></li><li><code>on_up_key</code>: <em>add</em> one to <code>state.player_y</code> (up means a bigger <code>y</code>)</li><li><code>on_down_key</code>: <em>subtract</em> one from <code>state.player_y</code></li></ul><p>The <code>def</code> lines are locked, so you can't accidentally rename them. <strong>Delete the <code>pass</code> line</strong> in each empty function before adding your code, otherwise it will stay there as a leftover (it still works, but it's tidier without it).</p>",
      "functions": [
        {"name": "on_left_key", "starterBody": "state.player_x -= 1"},
        {"name": "on_right_key", "starterBody": ""},
        {"name": "on_up_key", "starterBody": ""},
        {"name": "on_down_key", "starterBody": ""}
      ],
      "editorHeight": 12,
      "validation": {
        "rules": [
          { "type": "function_runs_clean" }
        ]
      }
    },
    {
      "id": "walls",
      "tier": "C",
      "selfCheck": true,
      "title": "Stop the player at the walls",
      "crossArea": true,
      "areaChips": ["on_left_key", "on_right_key", "on_up_key", "on_down_key"],
      "guidance": "<p>Right now the player can walk off the edge of the canvas. Fix that.</p><p>In each of the four key handlers (above), check the player's position before moving. If they're already at the edge, don't move them. For example, in <code>on_left_key</code>, only do <code>state.player_x -= 1</code> if <code>state.player_x &gt; 0</code>.</p><p>The canvas runs from <code>0</code> to <code>19</code> in both <code>x</code> and <code>y</code>.</p><p>Tick this card when the player can't walk off any edge.</p>"
    },
    {
      "id": "score",
      "tier": "A-B",
      "selfCheck": true,
      "title": "Add a score",
      "crossArea": true,
      "areaChips": ["State", "draw_scene", "on_*_key"],
      "guidance": "<p>Add a <code>state.score = 0</code> line to the <strong>State</strong> section. In one or more of your key handlers, bump the score (<code>state.score += 1</code>). In <code>draw_scene</code>, <code>print('Score:', state.score)</code> to show it under the canvas.</p><p>Variations: score only when you hit a target cell, score that resets when you press a particular key, score that grows a bar of cells. Up to you.</p><p>Tick this card when your score does something visible.</p>"
    },
    {
      "id": "freestyle",
      "tier": "A-B",
      "selfCheck": true,
      "title": "Invent something",
      "freestyle": true,
      "function": "your_additions",
      "areaDescription": "Free coding space. Code here runs at the top level after Setup. Add helpers, extra state, new functions, anything.",
      "guidance": "<p>Your turn. The editor on the right is a blank slate at the top level of your project. You can:</p><ul><li>Define a new helper function (e.g. <code>def draw_creature(x, y):</code>) and call it from <code>draw_scene</code></li><li>Add a new state variable and use it in your handlers</li><li>Compute something fancy using imports (<code>import random</code>, <code>import math</code>)</li><li>Anything else you want to try</li></ul><p>Tick this card when you've built something you're proud of.</p>",
      "starterBody": "# Your code here. Whatever you write runs at the top level\n# after Setup, before the function definitions.\n",
      "editorHeight": 8
    }
  ]
}
